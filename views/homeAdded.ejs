<!DOCTYPE html>
<html lang="en">
<%- include('header', {pageTitle: 'Property Added Successfully - Airbnb'}) %>
<body class="bg-gray-50 min-h-screen">
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-6">
                <a href="/" class="text-3xl font-bold text-red-500">airbnb</a>
                <nav class="space-x-6">
                    <a href="/" class="<%= activeTab === 'home' ? 'text-red-600 font-semibold border-b-2 border-red-600' : 'text-gray-700 hover:text-red-500' %> transition-colors pb-1">Home</a>
                    <a href="/browse" class="<%= activeTab === 'browse' ? 'text-red-600 font-semibold border-b-2 border-red-600' : 'text-gray-700 hover:text-red-500' %> transition-colors pb-1">Browse Properties</a>
                    <a href="/host/add-home" class="<%= activeTab === 'host' ? 'text-red-600 font-semibold border-b-2 border-red-600' : 'text-gray-700 hover:text-red-500' %> transition-colors pb-1">Host</a>
                </nav>
            </div>
        </div>
    </header>
    
    <main class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="bg-white rounded-lg shadow-md p-8 text-center">
            <div class="mb-6">
                <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-100 mb-4">
                    <svg class="h-8 w-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                </div>
                <h1 class="text-3xl font-bold text-gray-900 mb-4">Property Added Successfully!</h1>
                <p class="text-gray-600 mb-8">Your property has been registered and is now available for booking.</p>
            </div>
            
            <div class="space-y-4">
                <a href="/" class="inline-block bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors">
                    Go to Home
                </a>
                <div class="text-sm text-gray-500">
                    <a href="/host/add-home" class="hover:text-red-500 transition-colors">Add Another Property</a>
                </div>
            </div>
        </div>
    </main>
<%- include('footer') %>

